version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build:ci
  artifacts:
    baseDirectory: birzum/static
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

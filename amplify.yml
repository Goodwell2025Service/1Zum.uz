version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pnpm install --frozen-lockfile
    build:
      commands:
        - pnpm run build:ci
  artifacts:
    baseDirectory: birzum/static
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
